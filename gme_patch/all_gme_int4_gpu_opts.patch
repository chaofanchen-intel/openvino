diff --git a/src/plugins/intel_gpu/src/graph/impls/ocl_v2/sdpa/sdpa_gen_micro.cpp b/src/plugins/intel_gpu/src/graph/impls/ocl_v2/sdpa/sdpa_gen_micro.cpp
index 347b26d4ff305a1e3464c1a19c17a83d5ef6d9b0..1c8304568f1fa167592c5a3059170f1e9c9f3b03 100644
--- a/src/plugins/intel_gpu/src/graph/impls/ocl_v2/sdpa/sdpa_gen_micro.cpp
+++ b/src/plugins/intel_gpu/src/graph/impls/ocl_v2/sdpa/sdpa_gen_micro.cpp
@@ -143,6 +143,22 @@ inline size_t micro_get_num_heads(const kernel_impl_params& params, size_t qkv_i
     return -1;
 }
 
+// [batch, num_heads] of the SDPA result in logical (untransposed) order.
+//
+// The output layout already has output_transpose_order baked into it, so reading dims 0 and 1 off
+// it positionally only happens to be correct for the default order. Anything that sizes the
+// dispatch or the micro-kernel batch has to go through the order, exactly like the input side does.
+inline std::pair<int64_t, int64_t> micro_get_output_batch_and_num_heads(const kernel_impl_params& params) {
+    const auto& out = params.output_layouts[0];
+    if (params.is_type<scaled_dot_product_attention>()) {
+        const auto desc = params.typed_desc<scaled_dot_product_attention>();
+        const auto order = extend_order_in_num_heads_dim(desc->output_transpose_order);
+        return {get_batch_size(out, order), get_num_heads(out, order)};
+    }
+    const auto& out_ps = out.get_partial_shape();
+    return {out_ps[0].get_length(), out_ps[1].get_length()};
+}
+
 inline size_t micro_get_head_size(const kernel_impl_params& params, size_t qkv_idx) {
     if (params.is_type<paged_attention>()) {
         const auto desc = params.typed_desc<paged_attention>();
@@ -1038,14 +1054,14 @@ JitConstants SDPAMicroGenerator::get_jit_constants(const kernel_impl_params& par
     const auto& K = (config.is_paged_attention && !m_is_prefill) ? params.input_layouts[3] : params.input_layouts[1];
     const auto& V = (config.is_paged_attention && !m_is_prefill) ? params.input_layouts[4] : params.input_layouts[2];
     const auto& out = params.output_layouts[0];
-    const auto& out_ps = out.get_partial_shape();
 
     const auto head_size = micro_get_head_size(params, 0);
     const auto k_head_size = micro_get_head_size(params, 1);
     const auto v_head_size = micro_get_head_size(params, 2);
 
     const auto d_max = get_d_max(k_head_size);
-    const auto batch = out_ps[0] * out_ps[1];
+    const auto out_batch_heads = micro_get_output_batch_and_num_heads(params);
+    const auto batch = ov::Dimension(out_batch_heads.first) * ov::Dimension(out_batch_heads.second);
 
     auto ldq = k_head_size * ov::element::Type(Q.data_type).size();
     auto ldk = k_head_size * ov::element::Type(K.data_type).size();
@@ -1417,15 +1433,16 @@ DispatchDataFunc SDPAMicroGenerator::get_dispatch_data_func() const {
         scalars.clear();
         scalars.reserve(3);
 
-        auto params = impl_param;
+        // The stage was built from canonicalized (rank-4) shapes in SDPAOptImpl, but execute_stage
+        // hands the dispatch function the instance's raw params, which stay rank 3 for a headless
+        // SDPA. Everything below indexes layouts through a rank-4 transpose order, so canonicalize
+        // here the same way SDPAOptGenerator*::get_dispatch_data_func() does.
+        auto params = SDPABase::requires_shape_canonicalization(impl_param) ? SDPABase::static_canonicalize_shapes(impl_param) : impl_param;
         if (!params.is_dynamic()) {
             const auto& device_info = params.get_device_info();
             const auto& gemms = kd.micro_kernels;
             const auto& gemm_kq = gemms[kq_id]->p;
 
-            const auto& out = params.output_layouts[0];
-            const auto& out_ps = out.get_partial_shape();
-
             const auto v_head_size = micro_get_head_size(params, 2);
             auto head_num = micro_get_num_heads(params, 0);
 
@@ -1449,8 +1466,9 @@ DispatchDataFunc SDPAMicroGenerator::get_dispatch_data_func() const {
                 wgs.global[1] *= head_num;
                 wgs.global[2] *= 1;
             } else {
-                wgs.global[1] *= out_ps[1].get_length();
-                wgs.global[2] *= out_ps[0].get_length();
+                const auto out_batch_heads = micro_get_output_batch_and_num_heads(params);
+                wgs.global[1] *= out_batch_heads.second;
+                wgs.global[2] *= out_batch_heads.first;
             }
 
             auto to_int32 = [](size_t value) {
@@ -1500,8 +1518,6 @@ void SDPAMicroGenerator::init_microkernels(const kernel_impl_params& params,
     const auto& Q = params.input_layouts[0];
     const auto& K = (is_paged_attention && !is_prefill) ? params.input_layouts[3] : params.input_layouts[1];
     const auto& V = (is_paged_attention && !is_prefill) ? params.input_layouts[4] : params.input_layouts[2];
-    auto& out = params.output_layouts[0];
-    const auto& out_ps = out.get_partial_shape();
     const auto& device_info = params.get_device_info();
 
     const auto k_head_size = micro_get_head_size(params, 1);
@@ -1511,7 +1527,8 @@ void SDPAMicroGenerator::init_microkernels(const kernel_impl_params& params,
     const ov::Dimension n_keys = micro_get_seq_length(params, 1);
     const ov::Dimension n_queries = micro_get_seq_length(params, 0);
     const ov::Dimension n_values = ov::Dimension(v_head_size);
-    const auto batch = out_ps[0] * out_ps[1];
+    const auto out_batch_heads = micro_get_output_batch_and_num_heads(params);
+    const auto batch = ov::Dimension(out_batch_heads.first) * ov::Dimension(out_batch_heads.second);
 
     GPU_DEBUG_TRACE_DETAIL << "\nconfiguration.is_kv_compressed = " << configuration.is_kv_compressed << std::endl;
     GPU_DEBUG_TRACE_DETAIL << "k_head_size = " << k_head_size << ", v_head_size = " << v_head_size << ", d_max = " << d_max << ", batch = " << batch << "\n";
diff --git a/src/plugins/intel_gpu/src/graph/impls/onednn/fully_connected_onednn.cpp b/src/plugins/intel_gpu/src/graph/impls/onednn/fully_connected_onednn.cpp
index 92cc2bdb1c3542c48ea367677fca865678962c27..47c87bb267a93ba342feb152f2d3f86ec86d7ab3 100644
--- a/src/plugins/intel_gpu/src/graph/impls/onednn/fully_connected_onednn.cpp
+++ b/src/plugins/intel_gpu/src/graph/impls/onednn/fully_connected_onednn.cpp
@@ -98,6 +98,72 @@ protected:
         return args;
     }
 
+    // Compressed 4-bit weights arrive as a plain row-major [N, K] constant, which the matmul
+    // below describes as K-contiguous (`acb`).  When N is much larger than K the jit:gemm
+    // kernel is measurably faster with N-contiguous weights instead, so those are described as
+    // `abc` and transposed once by get_weights_reorder().
+    //
+    // The N >> K restriction is not cosmetic - the change is a clear loss outside it.  Measured
+    // per-matmul on a Qwen2-VL-2B INT4 prefill of 1024 tokens (Panther Lake iGPU, oneDNN
+    // verbose timings, cab -> abc):
+    //
+    //     wei 1536x8960   N/K 5.83    2.293 -> 1.798 ms   and   1.747 -> 1.293 ms
+    //     wei 1536x2048   N/K 1.33    0.474 -> 1.141 ms
+    //     wei 1536x1536   N/K 1.00    0.419 -> 0.486 ms
+    //
+    // The 4x threshold sits in the gap between the fastest measured win and the slowest
+    // measured loss; shapes below it keep the layout they arrive in.
+    //
+    // The same end-to-end sweep also shows the win is conditional on the row count, which is
+    // why the row check below exists (wall time, same model, layout on vs off):
+    //
+    //     32 rows 0.84x   64 rows 0.85x   128 rows 1.08x   256 rows 1.16x   1024 rows 1.11x
+    //
+    // The decision deliberately does not look at the weights *format*: get_weights_reorder()
+    // changes that format, and the primitive descriptor built afterwards has to agree with the
+    // one built before, or the matmul would read the reordered buffer with the old strides.
+    static bool use_n_contiguous_weights(const kernel_impl_params& impl_params,
+                                         size_t prim_input_size,
+                                         bool weights_transposed) {
+        if (prim_input_size != 3 || !weights_transposed)
+            return false;
+
+        const auto& weights_layout = impl_params.get_input_layout(1);
+        if (weights_layout.data_type != data_types::i4 && weights_layout.data_type != data_types::u4)
+            return false;
+        if (weights_layout.data_padding)
+            return false;
+
+        const auto& pshape = weights_layout.get_partial_shape();
+        if (pshape.size() != 2 || pshape.is_dynamic())
+            return false;
+
+        const auto N = pshape[0].get_length();
+        const auto K = pshape[1].get_length();
+        if (N < 4 * K)
+            return false;
+
+        // The layout only pays off once there are enough rows to amortize it; below that the
+        // original layout wins and the reorder is skipped entirely, so no second copy of the
+        // weights is allocated either.  The row count is taken as count()/K because the same
+        // projection is dispatched both as `1 x M x K` and as `M x 1 x K` depending on the
+        // node, and both mean M rows.
+        const auto& input_layout = impl_params.get_input_layout(0);
+        if (input_layout.is_dynamic())
+            return false;
+        constexpr int64_t min_rows = 128;
+        return static_cast<int64_t>(input_layout.count()) / K >= min_rows;
+    }
+
+    static std::shared_ptr<WeightsReorderParams> get_weights_reorder(const kernel_impl_params& impl_params) {
+        const auto& source_layout = impl_params.get_input_layout(1);
+        // Same logical [N, K] shape, only the physical order of the two axes changes:
+        // oiyx is K-innermost (what the constant already is), ioyx is N-innermost.
+        auto in_layout = layout(source_layout.get_partial_shape(), source_layout.data_type, format::oiyx);
+        auto out_layout = layout(source_layout.get_partial_shape(), source_layout.data_type, format::ioyx);
+        return std::make_shared<WeightsReorderParams>(in_layout, out_layout);
+    }
+
     static std::shared_ptr<dnnl::matmul::primitive_desc>
         get_matmul_primitive_descriptor(const kernel_impl_params& impl_params,
                                         cldnn::engine& engine,
@@ -162,7 +228,16 @@ protected:
         }
 
         dnnl::memory::desc weights_md;
-        if (weights_layout.data_padding
+        if (use_n_contiguous_weights(impl_params, prim_input_size, weights_transposed)) {
+            // get_weights_reorder() transposes the [N, K] constant to be N-innermost, which is
+            // exactly a dense `abc` descriptor over the (batch, K, N) matmul weights dims.
+            const auto& pshape = impl_params.get_input_layout(1).get_partial_shape();
+            const dnnl::memory::dim N = pshape[0].get_length();
+            const dnnl::memory::dim K = pshape[1].get_length();
+            weights_md = dnnl::memory::desc({1, K, N},
+                                            onednn::convert_data_type(weights_layout.data_type),
+                                            dnnl::memory::format_tag::abc);
+        } else if (weights_layout.data_padding
             && format::is_default_format(weights_layout.format)
             && (weights_layout.data_type == data_types::i4 || weights_layout.data_type == data_types::u4)) {
             weights_md = onednn::layout_to_memory_desc_strides(weights_layout, weights_fmt);
@@ -354,6 +429,12 @@ public:
         dnnl::memory::data_type dzp_data_type = dnnl::memory::data_type::undef;
         int idx = !arg.bias_term() ? 1 : 2;
 
+        // Must be decided from the same inputs as the matmul descriptor: the reorder rewrites
+        // the weights layout, so the two have to be kept in agreement.
+        std::shared_ptr<WeightsReorderParams> weights_reorder = nullptr;
+        if (use_n_contiguous_weights(impl_params, prim->input_size, prim->weights_transposed))
+            weights_reorder = get_weights_reorder(impl_params);
+
         if (prim->compressed_weights) {
             const auto input_dt = impl_params.get_input_layout(0).data_type;
             const bool is_dyn_quan_input = cldnn::one_of(input_dt, {data_types::i8, data_types::u8, data_types::f8e4m3, data_types::f8e5m2});
@@ -444,7 +525,7 @@ public:
             auto prim_desc = get_matmul_primitive_descriptor(impl_params, impl_params.prog->get_engine(),
                                                              prim->input_size, prim->weights_rank, prim->bias.is_valid(), *attr);
 
-            auto prim_onednn = std::make_unique<fully_connected_onednn>(engine, config, attr, *prim_desc);
+            auto prim_onednn = std::make_unique<fully_connected_onednn>(engine, config, attr, *prim_desc, weights_reorder);
             prim_onednn->_ds_group_size = group_size;
             prim_onednn->_ds_data_type = ds_data_type;
             prim_onednn->_dzp_data_type = dzp_data_type;
@@ -453,7 +534,7 @@ public:
             auto prim_desc = get_matmul_primitive_descriptor(impl_params, impl_params.prog->get_engine(),
                                                              prim->input_size, prim->weights_rank, prim->bias.is_valid(), *attr);
 
-            return std::make_unique<fully_connected_onednn>(engine, config, attr, *prim_desc);
+            return std::make_unique<fully_connected_onednn>(engine, config, attr, *prim_desc, weights_reorder);
         }
     }
 };
diff --git a/src/plugins/intel_gpu/src/graph/scaled_dot_product_attention.cpp b/src/plugins/intel_gpu/src/graph/scaled_dot_product_attention.cpp
index 828ddff651bfc32ba3ee1037878ccaa88e7fd41e..3b01a06f163d66fe28d864345e503646e78f7841 100644
--- a/src/plugins/intel_gpu/src/graph/scaled_dot_product_attention.cpp
+++ b/src/plugins/intel_gpu/src/graph/scaled_dot_product_attention.cpp
@@ -46,6 +46,12 @@ layout scaled_dot_product_attention_inst::calc_output_layout(scaled_dot_product_
     auto v_shape = transpose_shape(input2_layout.get_partial_shape(),
                                 desc->input_v_transpose_order);
     output_shape[output_shape.size() - 1] = v_shape[v_shape.size() - 1];
+    // output_transpose_order describes the permutation the SDPA applies on the way out, so the
+    // layout has to describe the permuted shape, exactly as calc_output_layouts() does through
+    // ov::intel_gpu::op::shape_infer(). Skipping it here left the legacy (static shape inference)
+    // path reporting the un-permuted shape, from which the SDPA implementations then read
+    // batch/num_heads off the wrong axes.
+    output_shape = transpose_shape(output_shape, desc->output_transpose_order);
 
     return { layout{output_shape, output_type, output_format, desc->output_paddings[0]} };
 }
diff --git a/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/reorder_weights_int4.cl b/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/reorder_weights_int4.cl
index 9332c491b3d7ca920e32fc24938665ad765a821c..0388a46b77afd7e864a0b051cb6f247f944c1a33 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/reorder_weights_int4.cl
+++ b/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/reorder_weights_int4.cl
@@ -49,6 +49,32 @@ KERNEL(reorder_weights_int4)(const __global INPUT0_TYPE* input, __global OUTPUT_
     INPUT0_TYPE in0 = (input[input0_offset / 2] >> input0_idx*4) & 0x0F;
     INPUT0_TYPE in1 = (input[input1_offset / 2] >> input1_idx*4) & 0x0F;
 
+    OUTPUT_TYPE out = in0 | (in1 << 4);
+    output[out_byte_offset] = out;
+#elif defined(INPUT0_LAYOUT_OIYX) && defined(OUTPUT_LAYOUT_IOYX)
+    // Mirror of the ioyx -> oiyx case above: transpose an unblocked 4-bit weight
+    // constant so that the output-feature axis becomes the innermost one.
+    const uint out_byte_offset = get_global_id(0);
+
+    const uint offset0 = out_byte_offset * 2 + 0;
+    const uint offset1 = out_byte_offset * 2 + 1;
+
+    // ioyx is o-innermost, so a flat output index decomposes as i * OFM + o.
+    const uint o0 = offset0 % OUTPUT_OFM_NUM;
+    const uint o1 = offset1 % OUTPUT_OFM_NUM;
+
+    const uint i0 = offset0 / OUTPUT_OFM_NUM;
+    const uint i1 = offset1 / OUTPUT_OFM_NUM;
+
+    const uint input0_offset = GET_FILTER_INDEX(INPUT0, 0, o0, i0, 0, 0);
+    const uint input1_offset = GET_FILTER_INDEX(INPUT0, 0, o1, i1, 0, 0);
+
+    const uint input0_idx = input0_offset % 2;
+    const uint input1_idx = input1_offset % 2;
+
+    INPUT0_TYPE in0 = (input[input0_offset / 2] >> input0_idx*4) & 0x0F;
+    INPUT0_TYPE in1 = (input[input1_offset / 2] >> input1_idx*4) & 0x0F;
+
     OUTPUT_TYPE out = in0 | (in1 << 4);
     output[out_byte_offset] = out;
 #elif defined(OUTPUT_LAYOUT_OS_IYX_OSV16)
diff --git a/src/plugins/intel_gpu/src/kernel_selector/kernels/reorder/reorder_weights_int4.cpp b/src/plugins/intel_gpu/src/kernel_selector/kernels/reorder/reorder_weights_int4.cpp
index 079da26f0ab7bb82881d80b28d925a788df73006..325849b617ac335eb10648a28eee662fe1c5533c 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/kernels/reorder/reorder_weights_int4.cpp
+++ b/src/plugins/intel_gpu/src/kernel_selector/kernels/reorder/reorder_weights_int4.cpp
@@ -24,6 +24,7 @@ ParamsKey ReorderWeightsKernelInt4::GetSupportedKey() const {
     k.EnableOutputWeightsLayout(WeightsLayout::os_iyx_osv64);
     k.EnableOutputWeightsLayout(WeightsLayout::os_is_yx_osv64_isv2);
     k.EnableOutputWeightsLayout(WeightsLayout::oiyx);
+    k.EnableOutputWeightsLayout(WeightsLayout::ioyx);
     k.EnableTensorOffset();
     k.EnableTensorPitches();
     return k;
@@ -60,6 +61,9 @@ ReorderWeightsKernelInt4::DispatchData ReorderWeightsKernelInt4::SetDefault(cons
         } else {
             dispatchData.gws = { CeilDiv(output.LogicalSize(), 2), 1, 1 };
         }
+    } else if (output.GetLayout() == WeightsLayout::ioyx) {
+        // One work item per output byte, i.e. per pair of packed 4-bit values.
+        dispatchData.gws = { CeilDiv(output.LogicalSize(), 2), 1, 1 };
     }
     dispatchData.lws = GetOptimalLocalWorkGroupSizes(dispatchData.gws, params.engineInfo);
 
@@ -132,6 +136,7 @@ bool ReorderWeightsKernelInt4::Validate(const Params& params) const {
     supported_case |= input.GetLayout() == WeightsLayout::oiyx && output.GetLayout() == WeightsLayout::os_iyx_osv64;
     supported_case |= input.GetLayout() == WeightsLayout::oiyx && output.GetLayout() == WeightsLayout::os_is_yx_osv64_isv2;
     supported_case |= input.GetLayout() == WeightsLayout::ioyx && output.GetLayout() == WeightsLayout::oiyx;
+    supported_case |= input.GetLayout() == WeightsLayout::oiyx && output.GetLayout() == WeightsLayout::ioyx;
     supported_case |= input.GetLayout() == WeightsLayout::ioyx && output.GetLayout() == WeightsLayout::os_iyx_osv32;
     return supported_case;
 }
diff --git a/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.cpp b/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.cpp
index 11ec247ffac133cbe4ff5b523c74a8ebfd6973a7..868d6a51ec0aa3a90882ad993191e5c538423d3c 100644
--- a/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.cpp
+++ b/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.cpp
@@ -3,6 +3,7 @@
 //
 
 #include "intel_gpu/op/gemm.hpp"
+#include "intel_gpu/op/indirect_sdpa.hpp"
 #include "intel_gpu/op/sdpa.hpp"
 #include "intel_gpu/runtime/utils.hpp"
 #include "openvino/core/node_vector.hpp"
@@ -77,6 +78,7 @@ bool has_optimized_version(const ov::Output<ov::Node>& output, bool supports_imm
 TransposeFusion::TransposeFusion(bool supports_immad) {
     add_matcher<TransposeMatMulTransposeMatcher>(supports_immad);
     add_matcher<TransposeMatMulMatcher>(supports_immad);
+    add_matcher<TransposeSDPATransposeMatcher>(supports_immad);
     add_matcher<TransposeSDPAMatcher>();
     add_matcher<TransposeVLSDPAMatcher>();
 }
@@ -310,6 +312,221 @@ TransposeSDPAMatcher::TransposeSDPAMatcher() {
     this->register_matcher(m, callback);
 }
 
+// Same as TransposeSDPAMatcher, but additionally absorbs the Transpose that consumes the SDPA
+// output into the SDPA's output_transpose_order. The micro SDPA kernel remaps the destination
+// strides from that order (convert_strides("DST", ...) in sdpa_gen_micro.cpp), so writing the
+// attention result out in the transposed layout is free there, whereas the standalone Transpose is
+// a full extra read+write of the [batch, heads, seq_len, head_size] tensor through a `permute_ref`
+// kernel. The canonical LLM/embedding attention block ends with exactly this
+// `SDPA -> Transpose(0, 2, 1, 3) -> Reshape -> o_proj` sequence.
+//
+// IMPORTANT: only the micro kernel honours output_transpose_order. sdpa_opt.cl and sdpa_ref.cl
+// write through OUTPUT_GET_INDEX(b, h, seq, head_size) with no OUTPUT_DIMS_ORDER remapping (unlike
+// the input side, which has INPUT{0,1,2}_DIMS_ORDER), so for them a non-default output order is
+// silently ignored and the result lands in the wrong place. The fusion is therefore restricted to
+// SDPAs that will run on the micro kernel -- see the guards in the callback below.
+TransposeSDPATransposeMatcher::TransposeSDPATransposeMatcher(bool supports_immad) {
+    auto is_fp_type = [](const ov::Output<ov::Node>& output) -> bool {
+        switch (output.get_element_type()) {
+            case ov::element::f16:
+            case ov::element::f32: return true;
+            default: return false;
+        }
+    };
+    auto not_transpose = [is_fp_type](const ov::Output<ov::Node>& output) -> bool {
+        return ov::as_type_ptr<ov::op::v1::Transpose>(output.get_node_shared_ptr()) == nullptr
+               && is_fp_type(output);
+    };
+
+    auto input_q_m = any_input(not_transpose);
+    auto input_k_m = any_input(not_transpose);
+    auto input_v_m = any_input(not_transpose);
+    auto input_attn_mask = any_input(not_transpose);
+    auto input_scale = any_input(not_transpose);
+    auto transpose_q_order_m = wrap_type<ov::op::v0::Constant>(consumers_count(1));
+    auto transpose_k_order_m = wrap_type<ov::op::v0::Constant>(consumers_count(1));
+    auto transpose_v_order_m = wrap_type<ov::op::v0::Constant>(consumers_count(1));
+    auto transpose_q_m = wrap_type<ov::op::v1::Transpose>({input_q_m, transpose_q_order_m}, is_fp_type);
+    auto transpose_k_m = wrap_type<ov::op::v1::Transpose>({input_k_m, transpose_k_order_m}, is_fp_type);
+    auto transpose_v_m = wrap_type<ov::op::v1::Transpose>({input_v_m, transpose_v_order_m}, is_fp_type);
+
+    auto sdpa_in_q = std::make_shared<Or>(OutputVector{input_q_m, transpose_q_m});
+    auto sdpa_in_k = std::make_shared<Or>(OutputVector{input_k_m, transpose_k_m});
+    auto sdpa_in_v = std::make_shared<Or>(OutputVector{input_v_m, transpose_v_m});
+
+    auto sdpa_without_attn_mask_m =
+        wrap_type<ov::op::v13::ScaledDotProductAttention>({ sdpa_in_q, sdpa_in_k, sdpa_in_v }, consumers_count(1));
+    auto sdpa_with_attn_mask_m =
+        wrap_type<ov::op::v13::ScaledDotProductAttention>({ sdpa_in_q, sdpa_in_k, sdpa_in_v, input_attn_mask }, consumers_count(1));
+    auto sdpa_with_attn_mask_and_scale_m =
+        wrap_type<ov::op::v13::ScaledDotProductAttention>({ sdpa_in_q, sdpa_in_k, sdpa_in_v, input_attn_mask, input_scale },
+                                                          consumers_count(1));
+
+    auto sdpa_m = std::make_shared<Or>(OutputVector{sdpa_without_attn_mask_m, sdpa_with_attn_mask_m, sdpa_with_attn_mask_and_scale_m});
+    auto transpose_out_order_m = wrap_type<ov::op::v0::Constant>(consumers_count(1));
+    auto transpose_out_m = wrap_type<ov::op::v1::Transpose>({sdpa_m, transpose_out_order_m}, is_fp_type);
+
+    ov::matcher_pass_callback callback = [OV_CAPTURE_CPY_AND_THIS](Matcher& m) {
+        const auto& pattern_map = m.get_pattern_value_map();
+
+        std::shared_ptr<ov::op::v13::ScaledDotProductAttention> sdpa;
+        for (const auto& sdpa_pattern : {sdpa_without_attn_mask_m, sdpa_with_attn_mask_m, sdpa_with_attn_mask_and_scale_m}) {
+            auto it = pattern_map.find(sdpa_pattern);
+            if (it != pattern_map.end()) {
+                sdpa = ov::as_type_ptr<ov::op::v13::ScaledDotProductAttention>(it->second.get_node_shared_ptr());
+                break;
+            }
+        }
+
+        if (!sdpa || transformation_callback(sdpa)) {
+            return false;
+        }
+
+        // Only the micro SDPA kernel reads output_transpose_order, and it is available on XMX
+        // devices only -- everywhere else SDPAOpt::supports_micro_sdpa() is false and every stage
+        // runs sdpa_opt, which would ignore the folded order.
+        if (!supports_immad) {
+            return false;
+        }
+
+        // SDPAOpt::supports_micro_sdpa() rejects a 3D SDPA outright ("Known limitation: In vision
+        // encoding model of qwen-vl ... restricted to execute with sdpa_opt kernel"), so a headless
+        // attention block -- the Qwen-VL / Qwen3-VL vision tower, whose SDPA is
+        // [num_heads, seq_len, head_size] -- can never honour a folded output order.
+        if (sdpa->get_output_partial_shape(0).size() != 4) {
+            return false;
+        }
+
+        // ov::intel_gpu::op::SDPA derives from ov::op::v13::ScaledDotProductAttention, and
+        // WrapType matches derived types, so the pattern above also matches an SDPA that a
+        // previous matcher already rewrote. This is the normal case: the SDPA node precedes its
+        // output Transpose in topological order, so TransposeSDPAMatcher has already run and has
+        // already absorbed the Q/K/V transposes into order_q/k/v. Those orders must be carried
+        // over -- rebuilding from default_order() while keeping the (already un-transposed)
+        // inputs silently reinterprets Q/K/V with the batch and seq_len axes swapped.
+        auto fused_sdpa = ov::as_type_ptr<op::SDPA>(sdpa);
+
+        // The 6-argument op::SDPA constructor below carries neither the beam table + indirect
+        // axis of IndirectSDPA nor the KV-cache quantization attributes and their scale/zp
+        // inputs, so those two shapes cannot be rebuilt faithfully. They are the stateful
+        // KV-cache paths, where the output Transpose is not on the critical path anyway.
+        if (ov::as_type_ptr<op::IndirectSDPA>(sdpa) || (fused_sdpa && fused_sdpa->get_kv_compressed())) {
+            return false;
+        }
+
+        auto order_q = fused_sdpa ? fused_sdpa->get_input0_transpose_order()
+                                  : op::SDPA::default_order(sdpa->get_input_partial_shape(0).size());
+        auto order_k = fused_sdpa ? fused_sdpa->get_input1_transpose_order()
+                                  : op::SDPA::default_order(sdpa->get_input_partial_shape(1).size());
+        auto order_v = fused_sdpa ? fused_sdpa->get_input2_transpose_order()
+                                  : op::SDPA::default_order(sdpa->get_input_partial_shape(2).size());
+        size_t input_q_output_idx = sdpa->get_input_source_output(0).get_index();
+        size_t input_k_output_idx = sdpa->get_input_source_output(1).get_index();
+        size_t input_v_output_idx = sdpa->get_input_source_output(2).get_index();
+
+        auto process_transpose = [](const std::shared_ptr<Node>& transpose_node,
+                                    const std::shared_ptr<Node>& transpose_order_const_node,
+                                    std::vector<int64_t>& order,
+                                    size_t& output_idx) {
+            auto transpose_order_const = ov::as_type_ptr<ov::op::v0::Constant>(transpose_order_const_node);
+
+            order = transpose_order_const->cast_vector<int64_t>();
+            // Allow any transposes without head_size dim position change
+            if (order.back() != static_cast<int64_t>(order.size() - 1))
+                return false;
+
+            auto transpose = ov::as_type_ptr<ov::op::v1::Transpose>(transpose_node);
+            output_idx = transpose->get_input_source_output(0).get_index();
+
+            return true;
+        };
+
+        bool can_fuse_transposes = true;
+        if (pattern_map.count(transpose_q_m) > 0)
+            can_fuse_transposes &= process_transpose(pattern_map.at(transpose_q_m).get_node_shared_ptr(),
+                                                     pattern_map.at(transpose_q_order_m).get_node_shared_ptr(),
+                                                     order_q, input_q_output_idx);
+
+        if (pattern_map.count(transpose_k_m) > 0)
+            can_fuse_transposes &= process_transpose(pattern_map.at(transpose_k_m).get_node_shared_ptr(),
+                                                     pattern_map.at(transpose_k_order_m).get_node_shared_ptr(),
+                                                     order_k, input_k_output_idx);
+
+        if (pattern_map.count(transpose_v_m) > 0)
+            can_fuse_transposes &= process_transpose(pattern_map.at(transpose_v_m).get_node_shared_ptr(),
+                                                     pattern_map.at(transpose_v_order_m).get_node_shared_ptr(),
+                                                     order_v, input_v_output_idx);
+
+        auto transpose_order =
+            ov::as_type_ptr<ov::op::v0::Constant>(pattern_map.at(transpose_out_order_m).get_node_shared_ptr())->cast_vector<int64_t>();
+        // Compose with the order the SDPA already emits instead of overwriting it. The node
+        // currently writes P[i] = L[prev[i]] for the logical BHLS result L, and the Transpose then
+        // produces R[i] = P[transpose_order[i]], so the fused order is prev[transpose_order[i]].
+        // prev is the identity unless this SDPA already had an output transpose folded in.
+        auto prev_order_output = fused_sdpa ? fused_sdpa->get_output_transpose_order()
+                                            : op::SDPA::default_order(sdpa->get_output_partial_shape(0).size());
+        std::vector<int64_t> order_output(transpose_order.size());
+        if (transpose_order.size() != prev_order_output.size() ||
+            transpose_order.size() != sdpa->get_output_partial_shape(0).size()) {
+            can_fuse_transposes = false;
+        } else {
+            for (size_t i = 0; i < transpose_order.size(); i++)
+                order_output[i] = prev_order_output[transpose_order[i]];
+        }
+
+        // The kernels index the destination by head_size-contiguous rows, so only orders that keep
+        // head_size as the innermost dimension can be folded into the output strides.
+        if (can_fuse_transposes && order_output.back() != static_cast<int64_t>(order_output.size() - 1))
+            can_fuse_transposes = false;
+
+        // convert_strides("DST", "OUTPUT", order) in the SDPA generators reads the destination
+        // strides as DST_S[i] = OUTPUT_pitch[order[i]], i.e. it treats the order as mapping the
+        // logical axis to the physical one, whereas shape_infer() applies it the other way round.
+        // The two agree only for self-inverse permutations -- which every real attention block
+        // uses, since it just swaps num_heads and seq_len -- so reject anything else rather than
+        // fold an order the kernels would then read backwards.
+        if (can_fuse_transposes) {
+            for (size_t i = 0; i < order_output.size(); i++) {
+                if (order_output[static_cast<size_t>(order_output[i])] != static_cast<int64_t>(i)) {
+                    can_fuse_transposes = false;
+                    break;
+                }
+            }
+        }
+
+        if (!can_fuse_transposes)
+            return false;
+
+        OutputVector inputs;
+        inputs.push_back(ov::Output<Node>(pattern_map.at(input_q_m).get_node_shared_ptr(), input_q_output_idx));
+        inputs.push_back(ov::Output<Node>(pattern_map.at(input_k_m).get_node_shared_ptr(), input_k_output_idx));
+        inputs.push_back(ov::Output<Node>(pattern_map.at(input_v_m).get_node_shared_ptr(), input_v_output_idx));
+
+        if (pattern_map.find(sdpa_with_attn_mask_m) != pattern_map.end()) {
+            inputs.push_back(sdpa->get_input_source_output(3));
+        } else if (pattern_map.find(sdpa_with_attn_mask_and_scale_m) != pattern_map.end()) {
+            inputs.push_back(sdpa->get_input_source_output(3));
+            inputs.push_back(sdpa->get_input_source_output(4));
+        }
+
+        auto sdpa_new = std::make_shared<op::SDPA>(inputs,
+                                                   sdpa->get_causal(),
+                                                   order_q,
+                                                   order_k,
+                                                   order_v,
+                                                   order_output,
+                                                   fused_sdpa ? fused_sdpa->get_output_type() : ov::element::dynamic);
+
+        sdpa_new->set_friendly_name(m.get_match_root()->get_friendly_name());
+        ov::copy_runtime_info(m.get_matched_nodes(), sdpa_new);
+        ov::replace_node(m.get_match_root(), sdpa_new);
+        return true;
+    };
+
+    auto m = std::make_shared<ov::pass::pattern::Matcher>(transpose_out_m, "TransposeSDPATransposeMatcher");
+    this->register_matcher(m, callback);
+}
+
 TransposeMatMulMatcher::TransposeMatMulMatcher(bool supports_immad) {
     auto not_transpose = [](const ov::Output<ov::Node>& output) -> bool {
         return ov::as_type_ptr<ov::op::v1::Transpose>(output.get_node_shared_ptr()) == nullptr
diff --git a/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.hpp b/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.hpp
index 2f0b45db43ddeb8291e7561df033969c5581498b..2b2d4f5e761b255ea50e6d405efb310f420fe5f6 100644
--- a/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.hpp
+++ b/src/plugins/intel_gpu/src/plugin/transformations/transpose_fusion.hpp
@@ -32,6 +32,12 @@ public:
     TransposeSDPAMatcher();
 };
 
+class TransposeSDPATransposeMatcher : public ov::pass::MatcherPass {
+public:
+    OPENVINO_MATCHER_PASS_RTTI("TransposeSDPATransposeMatcher");
+    TransposeSDPATransposeMatcher(bool supports_immad);
+};
+
 class TransposeVLSDPAMatcher : public ov::pass::MatcherPass {
 public:
     OPENVINO_MATCHER_PASS_RTTI("TransposeVLSDPAMatcher");
diff --git a/src/plugins/intel_gpu/src/plugin/transformations/unsqueeze_broadcast_reshape_sdpa_fusion.cpp b/src/plugins/intel_gpu/src/plugin/transformations/unsqueeze_broadcast_reshape_sdpa_fusion.cpp
index 6e922719981ff1ae762c777ea2a229537b16e927..c16d7c72fffab15ab181645000732da1d5964850 100644
--- a/src/plugins/intel_gpu/src/plugin/transformations/unsqueeze_broadcast_reshape_sdpa_fusion.cpp
+++ b/src/plugins/intel_gpu/src/plugin/transformations/unsqueeze_broadcast_reshape_sdpa_fusion.cpp
@@ -5,7 +5,6 @@
 #include "unsqueeze_broadcast_reshape_sdpa_fusion.hpp"
 
 #include "intel_gpu/op/sdpa.hpp"
-#include "intel_gpu/op/kv_cache.hpp"
 
 #include "openvino/core/rt_info.hpp"
 #include "openvino/op/broadcast.hpp"
@@ -40,8 +39,17 @@ UnsqueezeBroadcastReshapeSDPAFusion::UnsqueezeBroadcastReshapeSDPAFusion() {
     auto input_a_m = any_input();
     auto input_attn_mask_m = any_input();
     auto input_scale_m = any_input();
-    auto input_b_kvcache_m = wrap_type<ov::intel_gpu::op::KVCache>({any_input(), any_input()});
-    auto input_c_kvcache_m = wrap_type<ov::intel_gpu::op::KVCache>({any_input(), any_input()});
+    // The producer of the GQA repeat_kv expansion is a KVCache only in stateful generative
+    // models. A stateless graph -- an embedding/encoder forward pass, or any model compiled
+    // without a KV cache -- feeds the Unsqueeze straight from RoPE / Transpose / FullyConnected,
+    // so restricting it to KVCache leaves the whole Unsqueeze->Broadcast->Reshape chain in the
+    // runtime graph. The chain is materialized as a real `broadcast` primitive that writes an
+    // (num_heads / num_kv_heads)x larger K and V, which SDPA can broadcast internally for free.
+    // Everything the fusion actually depends on is verified in the callback below (bidirectional
+    // broadcast on exactly one axis, identical target shape for K and V), so the producer type
+    // itself carries no information and is left unconstrained.
+    auto input_b_kv_producer_m = any_input();
+    auto input_c_kv_producer_m = any_input();
 
     auto input_b_rope_m = wrap_type<ov::op::internal::RoPE>({any_input(), any_input(), any_input()});
     auto concat_b_m = optional<ov::op::v0::Concat>({any_input(), any_input()});
@@ -55,8 +63,8 @@ UnsqueezeBroadcastReshapeSDPAFusion::UnsqueezeBroadcastReshapeSDPAFusion() {
     auto axes_const_b_m = wrap_type<ov::op::v0::Constant>();
     auto axes_const_c_m = wrap_type<ov::op::v0::Constant>();
 
-    auto unsqueeze_b_m = wrap_type<ov::op::v0::Unsqueeze>({input_b_kvcache_m, axes_const_b_m}, unsqueeze_predicate);
-    auto unsqueeze_c_m = wrap_type<ov::op::v0::Unsqueeze>({input_c_kvcache_m, axes_const_c_m}, unsqueeze_predicate);
+    auto unsqueeze_b_m = wrap_type<ov::op::v0::Unsqueeze>({input_b_kv_producer_m, axes_const_b_m}, unsqueeze_predicate);
+    auto unsqueeze_c_m = wrap_type<ov::op::v0::Unsqueeze>({input_c_kv_producer_m, axes_const_c_m}, unsqueeze_predicate);
 
     auto pre_reshape_b_m = wrap_type<ov::op::v1::Reshape>({pre_reshape_input_b_m, any_input()}, unsqueeze_predicate);
     auto pre_reshape_c_m = wrap_type<ov::op::v1::Reshape>({pre_reshape_input_c_m, any_input()}, unsqueeze_predicate);

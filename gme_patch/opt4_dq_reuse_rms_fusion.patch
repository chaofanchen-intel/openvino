diff --git a/src/plugins/intel_gpu/include/intel_gpu/primitives/dynamic_quantize.hpp b/src/plugins/intel_gpu/include/intel_gpu/primitives/dynamic_quantize.hpp
index 4a2d6a29fef26d53f7ad83f0ff44a320a28398af..3e7d9213cab16338a4cd65680b675bdabc5d344a 100644
--- a/src/plugins/intel_gpu/include/intel_gpu/primitives/dynamic_quantize.hpp
+++ b/src/plugins/intel_gpu/include/intel_gpu/primitives/dynamic_quantize.hpp
@@ -48,9 +48,13 @@ struct dynamic_quantize : public primitive_base<dynamic_quantize> {
     Attributes attrs;
     size_t input_size;
     size_t innermost_size = 0;
+    bool fused_rms = false;
+    float rms_eps = 0.f;
 
     size_t hash() const override {
         size_t seed = primitive::hash();
+        seed = hash_combine(seed, fused_rms);
+        seed = hash_combine(seed, rms_eps);
         seed = hash_range(seed, attrs.scales_zp_output_order.begin(), attrs.scales_zp_output_order.end());
         seed = hash_range(seed, attrs.group_sizes.begin(), attrs.group_sizes.end());
         seed = hash_combine(seed, attrs.quantization_type);
@@ -81,6 +85,8 @@ struct dynamic_quantize : public primitive_base<dynamic_quantize> {
                attrs.quantization_type == rhs_casted.attrs.quantization_type &&
                attrs.precomputed_reduction == rhs_casted.attrs.precomputed_reduction &&
                attrs.precomputed_reduction_dt == rhs_casted.attrs.precomputed_reduction_dt &&
+               fused_rms == rhs_casted.fused_rms &&
+               rms_eps == rhs_casted.rms_eps &&
                input_size == rhs_casted.input_size &&
                innermost_size == rhs_casted.innermost_size;
     }
@@ -97,6 +103,8 @@ struct dynamic_quantize : public primitive_base<dynamic_quantize> {
         ob << attrs.scales_zp_output_order;
         ob << attrs.group_sizes;
         ob << attrs.precomputed_reduction;
+        ob << fused_rms;
+        ob << rms_eps;
         ob << input_size;
         ob << innermost_size;
     }
@@ -113,6 +121,8 @@ struct dynamic_quantize : public primitive_base<dynamic_quantize> {
         ib >> attrs.scales_zp_output_order;
         ib >> attrs.group_sizes;
         ib >> attrs.precomputed_reduction;
+        ib >> fused_rms;
+        ib >> rms_eps;
         ib >> input_size;
         ib >> innermost_size;
     }
diff --git a/src/plugins/intel_gpu/include/intel_gpu/runtime/options.inl b/src/plugins/intel_gpu/include/intel_gpu/runtime/options.inl
index 7ee83971272ed77b01f63d8bf5e79c27b0840595..8052287661f000f3fefd3d75f9db47c0009f17b0 100644
--- a/src/plugins/intel_gpu/include/intel_gpu/runtime/options.inl
+++ b/src/plugins/intel_gpu/include/intel_gpu/runtime/options.inl
@@ -59,7 +59,7 @@ OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, max_kernels_per_batch, 8, "Cont
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, impls_cache_capacity, 300, "Controls capacity of LRU implementations cache that is created for each program object for dynamic models")
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, asym_dynamic_quantization, false, "Enforce asymmetric mode for dynamically quantized activations")
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, could_use_flashattn_v2, true, "Enable/Disable SDPA primitive executing with FlashAttenV2 online softmax tricks.")
-OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, dynamic_quantization_threshold, 64, "Apply dynamic quantization only when batch size is larger than this value in OneDNN")
+OV_CONFIG_RELEASE_OPTION(ov::intel_gpu, dynamic_quantization_threshold, 64, "Apply dynamic quantization only when batch size is larger than this value in OneDNN")
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, dynamic_quantization_precomputed_reduction, true, "Precompute reduction of activation for faster dynamic quantization in case of asymmetric weight")
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, allow_bypass_xattn, true, "Allow bypass xattn execution if threshold >= 1.0.")
 OV_CONFIG_RELEASE_INTERNAL_OPTION(ov::intel_gpu, weightless_attr, nullptr, "Used to configure ov::WeightlessCacheAttribute for constants that are not loaded from a .bin file. This typically applies to non-IR inputs (e.g., ORT)")
diff --git a/src/plugins/intel_gpu/src/graph/dynamic_quantize.cpp b/src/plugins/intel_gpu/src/graph/dynamic_quantize.cpp
index 4d4442d4e89ba00f2722d083dbcbe817175a3115..a88eae83c9d618019b3f8eb4014571dbb9b2dba7 100644
--- a/src/plugins/intel_gpu/src/graph/dynamic_quantize.cpp
+++ b/src/plugins/intel_gpu/src/graph/dynamic_quantize.cpp
@@ -20,15 +20,20 @@ static bool should_skip_execution(const dynamic_quantize_node& node, const layou
         return false; // Execute unconditionally for floating point types.
     }
 
+    if (node.get_primitive()->fused_rms)
+        return false;
+
     if (!node.is_runtime_skippable()
         || !act_layout.is_static())
         return false;
 
-    // Do not skip dynamic quantization if next node is not fully connected.(such as SDPA)
-    OPENVINO_ASSERT(node.get_users().size() == node.get_outputs_count(),
-                    "Dynamic quantization is supposed to have only one user-node with duplicated connection: ", node.id());
-    if (!(*node.get_users().begin())->is_type<fully_connected>())
-        return false;
+    const size_t outputs_count = node.get_outputs_count();
+    OPENVINO_ASSERT(outputs_count > 0 && node.get_users().size() % outputs_count == 0,
+                    "Dynamic quantization users are not a whole multiple of its outputs: ", node.id());
+    for (const auto& user : node.get_users()) {
+        if (!user->is_type<fully_connected>())
+            return false;
+    }
 
     // If batch size is 1, dynamic_quantize is disabled for performance reason
     size_t input_batch = act_layout.batch();
diff --git a/src/plugins/intel_gpu/src/graph/graph_optimizer/prepare_primitive_fusing.cpp b/src/plugins/intel_gpu/src/graph/graph_optimizer/prepare_primitive_fusing.cpp
index a64fc77650b192ec252a4942ec0650f38bde4976..046ac5a30809dead4d04c572eb9380447f3646b9 100644
--- a/src/plugins/intel_gpu/src/graph/graph_optimizer/prepare_primitive_fusing.cpp
+++ b/src/plugins/intel_gpu/src/graph/graph_optimizer/prepare_primitive_fusing.cpp
@@ -45,6 +45,7 @@
 #include "group_normalization_inst.h"
 #include "lora_inst.h"
 #include "broadcast_inst.h"
+#include "dynamic_quantize_inst.h"
 #include <vector>
 #include <map>
 #include <list>
@@ -88,6 +89,89 @@ void prepare_primitive_fusing::run(program& p) {
     fuse_simple_primitives(p);
     fuse_constant_transposes(p);
     optimize_fused_ops(p);
+    fuse_rms_into_dynamic_quantize(p);
+}
+
+void prepare_primitive_fusing::fuse_rms_into_dynamic_quantize(program& p) {
+    // A folded node cannot switch back to the f16 pass-through used below the runtime
+    // quantization threshold. Only fold when the caller has disabled that bypass.
+    if (p.get_config().get_dynamic_quantization_threshold() != 0)
+        return;
+
+    auto iterator = p.get_processing_order().begin();
+    while (iterator != p.get_processing_order().end()) {
+        auto node = *iterator++;
+        if (!node->is_type<dynamic_quantize>() || node->is_output())
+            continue;
+
+        auto& dq_node = node->as<dynamic_quantize>();
+        auto dq_primitive = std::const_pointer_cast<dynamic_quantize>(dq_node.get_primitive());
+        if (dq_primitive->fused_rms || dq_node.has_fused_primitives() || dq_node.get_dependencies().size() != 1)
+            continue;
+
+        auto& rms_node = dq_node.get_dependency(0);
+        if (!rms_node.is_type<rms>() || rms_node.is_output() || rms_node.get_users().size() != 1)
+            continue;
+
+        auto rms_primitive = rms_node.as<rms>().get_primitive();
+        if (!rms_primitive->elementwise_affine || rms_node.get_dependencies().size() != 2)
+            continue;
+
+        if (rms_node.get_input_layouts()[0].data_type != data_types::f16 ||
+            rms_node.get_output_layout().data_type != data_types::f16 ||
+            dq_node.get_input_layouts()[0].data_type != data_types::f16)
+            continue;
+
+        using QuantizationType = ov::op::internal::DynamicQuantize::QuantizationType;
+        const auto& attributes = dq_primitive->attrs;
+        if (attributes.quantization_type != QuantizationType::Symmetric ||
+            attributes.quantization_dt != data_types::i8 ||
+            attributes.precomputed_reduction ||
+            attributes.group_sizes.empty() ||
+            attributes.group_sizes.back() != UINT64_MAX)
+            continue;
+
+        if (std::any_of(attributes.group_sizes.begin(),
+                        std::prev(attributes.group_sizes.end()),
+                        [](uint64_t group_size) { return group_size != 1; }))
+            continue;
+
+        if (!attributes.scales_zp_output_order.empty()) {
+            bool identity_order = true;
+            for (size_t index = 0; index < attributes.scales_zp_output_order.size(); index++)
+                identity_order &= attributes.scales_zp_output_order[index] == index;
+            if (!identity_order)
+                continue;
+        }
+
+        const auto& input_shape = rms_node.get_input_layouts()[0].get_partial_shape();
+        if (input_shape.rank().is_dynamic() || input_shape.size() == 0 ||
+            input_shape[input_shape.size() - 1].is_dynamic())
+            continue;
+
+        const auto row_size = static_cast<size_t>(input_shape[input_shape.size() - 1].get_length());
+        if (row_size != 1536)
+            continue;
+
+        auto& input_node = rms_node.get_dependency(0);
+        auto& gamma_node = rms_node.get_dependency(1);
+        const auto gamma_layout = gamma_node.get_output_layout();
+        if (gamma_layout.is_dynamic() || gamma_layout.count() != row_size ||
+            gamma_layout.data_type != data_types::f16)
+            continue;
+
+        dq_primitive->fused_rms = true;
+        dq_primitive->rms_eps = rms_primitive->epsilon;
+        dq_primitive->input = {input_info(input_node.id()), input_info(gamma_node.id())};
+
+        p.remove_connection(rms_node, dq_node);
+        p.add_connection(input_node, dq_node);
+        p.add_connection(gamma_node, dq_node);
+        if (p.remove_if_dangling(rms_node))
+            p.add_optimized_primitive_info(rms_primitive->id, {dq_primitive->id});
+
+        dq_node.recalc_output_layouts(false);
+    }
 }
 
 static std::optional<size_t> find_eltwise_const_dep_idx(const eltwise_node& node) {
diff --git a/src/plugins/intel_gpu/src/graph/impls/ocl/dynamic_quantize.cpp b/src/plugins/intel_gpu/src/graph/impls/ocl/dynamic_quantize.cpp
index b7c746cf860d56f62526401c004fc391584aeb8b..2c66e7f0da349c3b4558085f0e82654d1eead0c9 100644
--- a/src/plugins/intel_gpu/src/graph/impls/ocl/dynamic_quantize.cpp
+++ b/src/plugins/intel_gpu/src/graph/impls/ocl/dynamic_quantize.cpp
@@ -54,6 +54,14 @@ struct dynamic_quantize_impl : typed_primitive_impl_ocl<dynamic_quantize> {
         params.use_asymmetric_quantization = desc->attrs.quantization_type == ov::op::internal::DynamicQuantize::QuantizationType::Asymmetric;
         params.combine_scales_and_zp = desc->attrs.output_storage_type != ov::op::internal::DynamicQuantize::OutputStorageType::Planar;
         params.generate_precomputed_reduction = desc->attrs.precomputed_reduction;
+        params.fused_rms = desc->fused_rms;
+        params.rms_eps = desc->rms_eps;
+        if (desc->fused_rms) {
+            OPENVINO_ASSERT(impl_param.input_layouts.size() == 2,
+                            "[GPU] dynamic_quantize with fused RMSNorm must have exactly two inputs: ",
+                            impl_param.desc->id);
+            params.inputs.push_back(convert_data_tensor(impl_param.get_input_layout(1)));
+        }
 
         return params;
     }
diff --git a/src/plugins/intel_gpu/src/graph/include/pass_manager.h b/src/plugins/intel_gpu/src/graph/include/pass_manager.h
index 215b9712822ce1bb11a5d1cd21bbd256bc2ab709..f68720842b3608a9fb3e7426faafebf679dd6a30 100644
--- a/src/plugins/intel_gpu/src/graph/include/pass_manager.h
+++ b/src/plugins/intel_gpu/src/graph/include/pass_manager.h
@@ -158,6 +158,7 @@ private:
     void fuse_constant_transposes(program &p);
     void optimize_fused_ops(program &p);
     void remove_redundant_reshape(program &p);
+    void fuse_rms_into_dynamic_quantize(program &p);
 };
 
 class pre_replace_deconv : public base_pass {
diff --git a/src/plugins/intel_gpu/src/graph/primitive_inst.cpp b/src/plugins/intel_gpu/src/graph/primitive_inst.cpp
index 95cb7470fda255fbb9666462b3c455613d296469..654061e0d853b2c2a6a17adc6624eb0daec3749a 100644
--- a/src/plugins/intel_gpu/src/graph/primitive_inst.cpp
+++ b/src/plugins/intel_gpu/src/graph/primitive_inst.cpp
@@ -542,7 +542,8 @@ void primitive_inst::update_shape() {
         auto &layout = _impl_params->get_output_layout(0);
         OPENVINO_ASSERT(one_of(layout.data_type, {data_types::f16, data_types::i8, data_types::u8, data_types::f8e4m3, data_types::f8e5m2}),
             "[GPU] Unsupported data type of dynamic_quantize: ", layout.data_type);
-        if (layout.data_type == data_types::f16)
+        const bool fused_rms = get_node().as<dynamic_quantize>().get_primitive()->fused_rms;
+        if (layout.data_type == data_types::f16 && !fused_rms)
             set_can_be_optimized(true);
         else
             set_can_be_optimized(false);
diff --git a/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/dynamic_quantize_gpu_opt.cl b/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/dynamic_quantize_gpu_opt.cl
index 80bed3e059b05c743f87bf64d6acedc62aa9d56c..4040534048f3a4d346ec8dfb3aff76170d58fb5a 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/dynamic_quantize_gpu_opt.cl
+++ b/src/plugins/intel_gpu/src/kernel_selector/cl_kernels/dynamic_quantize_gpu_opt.cl
@@ -299,11 +299,17 @@ KERNEL(dynamic_quantize_gpu_opt)(
 #if GENERATE_PRECOMPUTED_REDUCTION != 0
 #error "UNIMPLMENTED: precomputed_reduction generation"
 #endif
+#if FUSE_RMS && (ASYMMETRIC_QUANTIZATION != 0 || IS_F8 != 0 || IS_MXFP != 0)
+#error "FUSE_RMS requires per-token symmetric int8 quantization"
+#endif
 
 REQD_SUB_GROUP_SIZE(SIMD)
 KERNEL(dynamic_quantize_gpu_opt)(
     OPTIONAL_SHAPE_INFO_ARG
     const __global INPUT0_TYPE* input,
+#if FUSE_RMS
+    const __global INPUT1_TYPE* gamma,
+#endif
     __global OUTPUT_TYPE* output,
     __global OUTPUT1_TYPE* output_scale
 #if ASYMMETRIC_QUANTIZATION
@@ -311,6 +317,111 @@ KERNEL(dynamic_quantize_gpu_opt)(
 #endif
     )
 {
+#if FUSE_RMS
+    const uint bf = (uint)get_global_id(2);
+    const uint lane = get_sub_group_local_id();
+    const uint sub_group = (uint)get_local_id(1);
+#if OUTPUT_DIMS == 2
+    const uint row_offset = bf * INPUT0_BATCH_PITCH;
+#else
+    const uint row_offset = bf * INPUT0_FEATURE_PITCH;
+#endif
+    const uint sub_group_offset = sub_group * SIMD * RMS_ITEMS_NUM;
+
+    float rms_accumulator = 0.0f;
+    float rms_data[RMS_ITEMS_NUM];
+    half normalized[RMS_ITEMS_NUM];
+    __local float rms_slm[RMS_SUB_GROUP_NUM];
+    __local half range_max_slm[RMS_SUB_GROUP_NUM];
+    __local half range_min_slm[RMS_SUB_GROUP_NUM];
+
+    unroll_for (uint item = 0; item < RMS_ITEMS_NUM; item++) {
+        const uint index = sub_group_offset + lane + item * SIMD;
+        float value = convert_float(input[row_offset + index]);
+        rms_data[item] = value;
+        rms_accumulator += native_powr(value, 2);
+    }
+
+    rms_accumulator = sub_group_reduce_add(rms_accumulator);
+    if (lane == 0)
+        rms_slm[sub_group] = rms_accumulator;
+    barrier(CLK_LOCAL_MEM_FENCE);
+
+    const uint flat_local_id = sub_group * SIMD + lane;
+    for (uint offset = RMS_SUB_GROUP_NUM / 2; offset > 0; offset /= 2) {
+        if (flat_local_id < offset)
+            rms_slm[flat_local_id] += rms_slm[flat_local_id + offset];
+        barrier(CLK_LOCAL_MEM_FENCE);
+    }
+
+    if (flat_local_id == 0) {
+        float mean_square = rms_slm[0] / RMS_DATA_SIZE;
+        rms_slm[0] = native_powr(sqrt(mean_square + convert_float(RMS_EPSILON)), -1);
+    }
+    barrier(CLK_LOCAL_MEM_FENCE);
+
+    half local_max = ACT_MIN_VAL;
+    half local_min = ACT_MIN_VAL;
+    unroll_for (uint item = 0; item < RMS_ITEMS_NUM; item++) {
+        const uint index = sub_group_offset + lane + item * SIMD;
+        normalized[item] = convert_half(rms_slm[0] * rms_data[item] * convert_float(gamma[index]));
+#if ASYMMETRIC_QUANTIZATION
+        local_max = fmax(local_max, normalized[item]);
+        local_min = fmin(local_min, normalized[item]);
+#else
+        local_max = fmax(local_max, fabs(normalized[item]));
+#endif
+    }
+
+    local_max = sub_group_reduce_max(local_max);
+#if ASYMMETRIC_QUANTIZATION
+    local_min = sub_group_reduce_min(local_min);
+#endif
+    if (lane == 0) {
+        range_max_slm[sub_group] = local_max;
+#if ASYMMETRIC_QUANTIZATION
+        range_min_slm[sub_group] = local_min;
+#endif
+    }
+    barrier(CLK_LOCAL_MEM_FENCE);
+
+    if (sub_group == 0) {
+        half row_max = sub_group_reduce_max(range_max_slm[lane]);
+#if ASYMMETRIC_QUANTIZATION
+        half row_min = sub_group_reduce_min(range_min_slm[lane]);
+#endif
+        if (lane == 0) {
+            range_max_slm[0] = row_max;
+#if ASYMMETRIC_QUANTIZATION
+            range_min_slm[0] = row_min;
+#endif
+        }
+    }
+    barrier(CLK_LOCAL_MEM_FENCE);
+
+#if ASYMMETRIC_QUANTIZATION
+    OUTPUT1_TYPE scale = (OUTPUT1_TYPE)((CHAR_MAX - CHAR_MIN) / (range_max_slm[0] - range_min_slm[0]));
+    OUTPUT2_TYPE zp = (OUTPUT2_TYPE)(-range_min_slm[0] * scale);
+#else
+    SCALE_TYPE scale = TO_SCALE_TYPE(OUTPUT_VAL_MAX) / range_max_slm[0];
+#endif
+
+    unroll_for (uint item = 0; item < RMS_ITEMS_NUM; item++) {
+        const uint index = sub_group_offset + lane + item * SIMD;
+#if ASYMMETRIC_QUANTIZATION
+        output[row_offset + index] = convert_uchar_rte(normalized[item] * scale + zp);
+#else
+        output[row_offset + index] = convert_char_rte(normalized[item] * scale);
+#endif
+    }
+
+    if (flat_local_id == 0) {
+        output_scale[bf] = 1.0h / scale;
+#if ASYMMETRIC_QUANTIZATION
+        output_zp[bf] = convert_uchar_rte(zp);
+#endif
+    }
+#else
     const uint bf = (uint)get_global_id(2);
     const uint sglid = get_sub_group_local_id();
     const uint local_id = (uint)get_local_id(1);
@@ -340,6 +451,12 @@ KERNEL(dynamic_quantize_gpu_opt)(
             continue;
 
         val[i] = AS_INPUT_TYPE_N(VLOAD_N(0, input + offset + ((local_id * iteration + i) * block_size)));
+    }
+
+    unroll_for(int i = 0; i < iteration; ++i) {
+        if ((local_id * iteration + i) >= TOTAL_BLOCK_NUM)
+            continue;
+
 #if ASYMMETRIC_QUANTIZATION
         unroll_for (int j = 0; j < VEC_SIZE; j++) {
             max_value = fmax(max_value, val[i][j]);
@@ -408,6 +525,7 @@ KERNEL(dynamic_quantize_gpu_opt)(
         output_zp[bf] = convert_uchar_rte(zp);
 #endif
     }
+#endif
 }
 
 #else   // DYNAMIC_QUANTIZAION_IMPL_MODE
diff --git a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt.cpp b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt.cpp
index 9ee351365b54fa32fb044407c2e1d74ef94f3f32..c19ab638a5641ed73e5abb725ae2b64a86ddc2b1 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt.cpp
+++ b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt.cpp
@@ -111,8 +111,15 @@ JitConstants DynamicQuantizeKernelOpt::GetJitConstants(const dynamic_quantize_pa
     jit.AddConstant(MakeJitConstant("IS_MXFP", params.outputs[1].GetDType() == Datatype::F8E8M0 ? 1 : 0));
     jit.Merge(GetTensorFriendlyWorkGroupsJit(params.outputs[0]));
     jit.AddConstant(MakeJitConstant("TOTAL_BLOCK_NUM", total_block_num));
-    size_t block_num = (total_block_num > 32) ? 32 : total_block_num;
+    size_t block_num = params.fused_rms ? 16 : ((total_block_num > 32) ? 32 : total_block_num);
     jit.AddConstant(MakeJitConstant("BLOCK_NUM", block_num));
+    jit.AddConstant(MakeJitConstant("FUSE_RMS", params.fused_rms));
+    if (params.fused_rms) {
+        jit.AddConstant(MakeJitConstant("RMS_EPSILON", params.rms_eps));
+        jit.AddConstant(MakeJitConstant("RMS_DATA_SIZE", bf_size.second));
+        jit.AddConstant(MakeJitConstant("RMS_ITEMS_NUM", 6));
+        jit.AddConstant(MakeJitConstant("RMS_SUB_GROUP_NUM", 16));
+    }
 
     if (mode == DynQuanMode::PER_TOKEN)  {
         size_t aligned_block_num = (total_block_num > 32) ? Align(total_block_num, 32) : total_block_num;
@@ -126,6 +133,13 @@ CommonDispatchData DynamicQuantizeKernelOpt::SetDefault(const dynamic_quantize_p
     CommonDispatchData dispatchData;
     auto mode = get_dynamic_quantize_mode(params);
 
+    if (params.fused_rms) {
+        auto bf_size = get_input_bf_size(params);
+        dispatchData.gws = {simd, 16, bf_size.first};
+        dispatchData.lws = {simd, 16, 1};
+        return dispatchData;
+    }
+
     if (mode == DynQuanMode::SMALL_GS) {
         auto bf_size = get_input_bf_size(params);
         dispatchData.gws = {bf_size.first, bf_size.second / params.group_sizes.back(), 1};
@@ -196,7 +210,7 @@ KernelsData DynamicQuantizeKernelOpt::GetKernelsData(const Params& params) const
                      EXE_MODE_DEFAULT,
                      false,
                      false,
-                     1,
+                     static_cast<int>(prim_params.inputs.size()),
                      GetFusedPrimitiveInputsCount(params),
                      static_cast<int>(prim_params.outputs.size()),
                      prim_params.is_shape_agnostic);
@@ -250,6 +264,22 @@ bool DynamicQuantizeKernelOpt::Validate(const Params& params) const {
     if (dq_params.append_axis != -1)
         DO_NOT_USE_THIS_KERNEL(params.layerID);
 
+    if (dq_params.fused_rms) {
+        if (dq_params.inputs.size() != 2 ||
+            get_dynamic_quantize_mode(dq_params) != DynQuanMode::PER_TOKEN ||
+            dq_params.use_asymmetric_quantization ||
+            dq_params.generate_precomputed_reduction)
+            DO_NOT_USE_THIS_KERNEL(params.layerID);
+
+        if (dq_params.outputs[0].GetDType() != Datatype::INT8)
+            DO_NOT_USE_THIS_KERNEL(params.layerID);
+
+        auto vec_size = get_match_vector_size(dq_params);
+        auto bf = get_input_bf_size(dq_params);
+        if (vec_size != 8 || bf.second != 1536 || dq_params.inputs[1].LogicalSize() != bf.second)
+            DO_NOT_USE_THIS_KERNEL(params.layerID);
+    }
+
     for (size_t i = 0; i < dq_params.group_sizes.size() - 1; i++) {
         if (dq_params.group_sizes[i] != 1)
             DO_NOT_USE_THIS_KERNEL(params.layerID);
diff --git a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt_kv_cache.cpp b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt_kv_cache.cpp
index c0a47c23dc2641d29d64c33bc54f7b214ecc35fe..1731ed109836a5471996e3005f3b1299e740f361 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt_kv_cache.cpp
+++ b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_opt_kv_cache.cpp
@@ -269,6 +269,9 @@ bool DynamicQuantizeKernelKVCache::Validate(const Params& params) const {
     if (dq_params.generate_precomputed_reduction)
         DO_NOT_USE_THIS_KERNEL(params.layerID);
 
+    if (dq_params.fused_rms)
+        DO_NOT_USE_THIS_KERNEL(params.layerID);
+
     if (non_compressed_dims_number == group_sizes.size())
         DO_NOT_USE_THIS_KERNEL(params.layerID);
 
diff --git a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.cpp b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.cpp
index 4b36c2eded23baf49a8f27abe0cc642ef249a185..68d2b9f724200d4fee4e2415d6400bb33f2a99bc 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.cpp
+++ b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.cpp
@@ -167,6 +167,9 @@ bool DynamicQuantizeKernelRef::Validate(const Params& params) const {
         DO_NOT_USE_THIS_KERNEL(params.layerID);
     }
 
+    if (dq_params.fused_rms)
+        DO_NOT_USE_THIS_KERNEL(params.layerID);
+
     return true;
 }
 }  // namespace kernel_selector
diff --git a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.h b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.h
index 6b7985aef25b854e91963ef87cbf95dd51920d7f..242bdd3ad443210ad229e874306f6a8371bbe848 100644
--- a/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.h
+++ b/src/plugins/intel_gpu/src/kernel_selector/kernels/dynamic_quantize/dynamic_quantize_kernel_ref.h
@@ -22,6 +22,8 @@ struct dynamic_quantize_params : public base_params {
     bool combine_scales_and_zp = false;
     bool generate_precomputed_reduction = false;
     bool is_int4_compressed = false;
+    bool fused_rms = false;
+    float rms_eps = 0.f;
 };
 
 class DynamicQuantizeKernelRef : public KernelBaseOpenCL {
diff --git a/src/plugins/intel_gpu/src/plugin/plugin.cpp b/src/plugins/intel_gpu/src/plugin/plugin.cpp
index 2941627f49edf66906e416ad9b4bee8c28d11702..d2a1f96f5a02837c03a295ef77ac4ba91687b696 100644
--- a/src/plugins/intel_gpu/src/plugin/plugin.cpp
+++ b/src/plugins/intel_gpu/src/plugin/plugin.cpp
@@ -726,6 +726,7 @@ std::vector<ov::PropertyName> Plugin::get_caching_properties() const {
         ov::PropertyName{ov::hint::execution_mode.name(), PropertyMutability::RW},
         ov::PropertyName{ov::hint::performance_mode.name(), PropertyMutability::RW},
         ov::PropertyName{ov::hint::dynamic_quantization_group_size.name(), PropertyMutability::RW},
+        ov::PropertyName{ov::intel_gpu::dynamic_quantization_threshold.name(), PropertyMutability::RW},
         ov::PropertyName{ov::hint::activations_scale_factor.name(), PropertyMutability::RW},
     };
 
@@ -781,6 +782,7 @@ std::vector<ov::PropertyName> Plugin::get_supported_properties() const {
         ov::PropertyName{ov::hint::enable_cpu_reservation.name(), PropertyMutability::RW},
         ov::PropertyName{ov::device::id.name(), PropertyMutability::RW},
         ov::PropertyName{ov::hint::dynamic_quantization_group_size.name(), PropertyMutability::RW},
+        ov::PropertyName{ov::intel_gpu::dynamic_quantization_threshold.name(), PropertyMutability::RW},
         ov::PropertyName{ov::hint::activations_scale_factor.name(), PropertyMutability::RW},
         ov::PropertyName{ov::weights_path.name(), PropertyMutability::RW},
         ov::PropertyName{ov::cache_encryption_callbacks.name(), PropertyMutability::WO},
diff --git a/src/plugins/intel_gpu/src/plugin/transformations/dynamic_quantize_fully_connected.cpp b/src/plugins/intel_gpu/src/plugin/transformations/dynamic_quantize_fully_connected.cpp
index 58ed3e3834b5d5a6857a69ef3290356035db4b94..476637c6f87da11e06c8609e4a1bf9e0093ffdd2 100644
--- a/src/plugins/intel_gpu/src/plugin/transformations/dynamic_quantize_fully_connected.cpp
+++ b/src/plugins/intel_gpu/src/plugin/transformations/dynamic_quantize_fully_connected.cpp
@@ -18,6 +18,47 @@
 
 namespace ov::intel_gpu {
 
+namespace {
+bool output_orders_equal(const std::vector<uint64_t>& lhs, const std::vector<uint64_t>& rhs) {
+    if (lhs == rhs)
+        return true;
+
+    const auto is_identity = [](const std::vector<uint64_t>& order) {
+        for (size_t index = 0; index < order.size(); index++) {
+            if (order[index] != index)
+                return false;
+        }
+        return true;
+    };
+    return (lhs.empty() && is_identity(rhs)) || (rhs.empty() && is_identity(lhs));
+}
+
+bool dq_attrs_equal(const ov::op::internal::DynamicQuantize::Attributes& lhs,
+                    const ov::op::internal::DynamicQuantize::Attributes& rhs) {
+    return lhs.quantization_type == rhs.quantization_type &&
+           lhs.quantization_dt == rhs.quantization_dt &&
+           lhs.scale_dt == rhs.scale_dt &&
+           lhs.zp_dt == rhs.zp_dt &&
+           lhs.precomputed_reduction == rhs.precomputed_reduction &&
+           lhs.precomputed_reduction_dt == rhs.precomputed_reduction_dt &&
+           lhs.output_storage_type == rhs.output_storage_type &&
+           output_orders_equal(lhs.scales_zp_output_order, rhs.scales_zp_output_order) &&
+           lhs.group_sizes == rhs.group_sizes;
+}
+
+std::shared_ptr<ov::op::internal::DynamicQuantize> find_reusable_dq(
+    const ov::Output<ov::Node>& activation,
+    const ov::op::internal::DynamicQuantize::Attributes& config) {
+    for (const auto& target_input : activation.get_target_inputs()) {
+        auto candidate = ov::as_type_ptr<ov::op::internal::DynamicQuantize>(
+            target_input.get_node()->shared_from_this());
+        if (candidate && dq_attrs_equal(candidate->get_attrs(), config))
+            return candidate;
+    }
+    return nullptr;
+}
+}  // namespace
+
 // precomputed_reduction is providing partial reduction of activation from dynamic quantization into onednn for faster computation
 // It is used for asymmetric weight.
 DynamicQuantizeFullyConnected::DynamicQuantizeFullyConnected(uint64_t group_size,
@@ -116,7 +157,9 @@ DynamicQuantizeFullyConnected::DynamicQuantizeFullyConnected(uint64_t group_size
             config.zp_dt = element::u8; // it supports u8 only now
         }
 
-        std::shared_ptr<ov::op::internal::DynamicQuantize> dyn_quan = std::make_shared<ov::op::internal::DynamicQuantize>(m_fc->input_value(0), config);
+        auto dyn_quan = find_reusable_dq(m_fc->input_value(0), config);
+        if (!dyn_quan)
+            dyn_quan = std::make_shared<ov::op::internal::DynamicQuantize>(m_fc->input_value(0), config);
 
         int dyn_quan_output_idx = 2;
         auto optional_a_zp = config.quantization_type == QuantizationType::Symmetric ?
